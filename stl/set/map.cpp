#include<bits/stdc++.h>
using namespace std;
int n;
string gifter;
map<int, string> ppl;
map<int, string> ::iterator it;

int main() {
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> ppl[i];
    }
    cin >> gifter;
}