#include <bits/stdc++.h>
using namespace std;
const int MAXN =1e9;
int n, k;
long long hi[MAXN], card[MAXN];

int main() {
    cin >> n >> k;
    for (int i = 1; i <= n; i++) {
        cin >> hi[i];
    }
    for (int i = 1; i <= k; i++) {
        cin >> card[i];
    }
    
}