#include <bits/stdc++.h>
using namespace std;
int n, na, nb;
vector<int> a;
vector<int> b;
// UNFINISHED.
int main() {
    cin >> n >> na >> nb;
    for (int i = 0; i < na; i++) {
        int x;
        cin >> x;
        a.push_back(x);
    }
    for (int i = 0; i < nb; i++) {
        int x;
        cin >> x;
        b.push_back(x);
    }
}